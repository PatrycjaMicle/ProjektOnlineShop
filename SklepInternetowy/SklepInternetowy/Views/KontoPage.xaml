<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:SklepInternetowy.ViewModels;assembly=SklepInternetowy"
             x:Class="SklepInternetowy.Views.KontoPage">

    <ContentPage.BindingContext>
        <viewModels:KontoViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="2*" />
            </Grid.RowDefinitions>

            <StackLayout Margin="20">
                <Label Text="{Binding Header}" FontSize="Header" />
                <Label Text="Tutaj możesz zarządzać swoimi, zamówieniami i informacjami o koncie." FontSize="Body" />
            </StackLayout>

            <StackLayout Grid.Row="1" Margin="10">
                <Button Text="Dane osobowe" Command="{Binding GoToDaneOsoboweCommand}" />
                <Button Text="Adresy" Command="{Binding GoToAdresyCommand}" />
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>